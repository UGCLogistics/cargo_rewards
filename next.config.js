/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Enable experimental server actions if needed
}

module.exports = nextConfig;

// next.config.js
module.exports = {
  images: {
    domains: ["mhzymxqcfrmswjdydtbt.supabase.co"],
  },
};
